# Configuration for fund-investment-tracker (MVP)
# Adjust concurrency and connector retry policies here.
concurrency:
  max_workers: 3

connectors:
  default_max_retries: 2
  default_backoff_seconds: 1.0
